import 'package:flutter/material.dart';

class Chat_screan extends StatelessWidget {
  const Chat_screan({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Chat_screan'));
  }
}